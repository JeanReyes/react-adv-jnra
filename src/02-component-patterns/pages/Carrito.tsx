import React from 'react'
import { CarritoProps } from './ShoppingPage'

export const Carrito = ({children, products}:CarritoProps) => {
    console.log(products);
    
  return (
        <>
            {/* {products > 0 && children} */}
        </>
    )
}
